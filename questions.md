# Открытые вопросы по миграции vsforni.ru

> Файл ведётся автоматически в ходе миграции.

---

## Фаза 0 — ЗАКРЫТО

### Q1 — ЗАКРЫТО
Изображения `img/catalog/` добавлены (110 файлов в content/img/catalog/).

### Q2 — ЗАКРЫТО
4 продукта с пустым pagetitle в «Автоматических линиях»: заголовок берётся из родительской группы.

### Q3 — ЗАКРЫТО
Категория «Тестоподготовка» (URI `testomesyi,-mikseryi/`): делаем 301.
Новый URL: `testopodgotovka/`. Все дочерние страницы получают новые URL,
старые URL → 301 редиректы.

---

## Фаза 1

### Q4 — 1 продукт без спецификаций

id=258 «Автоматическое производство вафельных трубок VSF с начинкой»
(`konditerskoe-oborudovanie/proizvodstvo-vafelnyix-trubok-s-nachinkoj/...`)
не имеет TV с таблицей характеристик. Это нормально (описательная страница без табличных данных)?
Если да — оставляем как есть, спецификации будут пустыми.

### Q6 — 12 URL есть в sitemap, но не в дампе БД

Два случая разной природы:

**a) published=0 в дампе:** Группы «Vilochnyij-testomes» (id=52, 7 страниц)
и «Rotaczionnaya-pech-s-frontalnyim» (id=19, 5 страниц) — в дампе помечены
`published=0`, но присутствуют в sitemap живого сайта.
→ Нужно: убедиться, что страницы опубликованы на живом сайте, и передать
обновлённый дамп. Или разрешить их пропустить (они получат 404 на новом сайте).

**b) id=263** — нестандартная страница (template=5 + isfolder=1):
«Экструзионно-отсадочная машина с диафрагменной резкой».
Уже исправлено в extract.ts, теперь включена как группа.

### Q10 — ID счётчика Яндекс.Метрики

Компонент `YandexMetrika.astro` уже встроен с deferred loading.
Нужно: добавить в `.env` переменную `YANDEX_METRIKA_ID=XXXXXXXX`
с реальным номером счётчика.

### Q8 — Коды верификации поисковиков

Для завершения SEO нужны файлы верификации. Предоставь коды:
- **Yandex.Webmaster** — HTML-файл вида `yandex_XXXXXXXX.html` или meta-тег в <head>
- **Google Search Console** — HTML-файл вида `googleXXXXXXXX.html` или meta-тег
- Yandex Turbo — если используется (обычно настраивается в Яндекс.Вебмастере)

Положи файлы верификации в `public/` и сообщи мне — добавлю meta-теги в BaseLayout.

### Q9 — Ссылки на соцсети для Organization JSON-LD

В `src/components/layout/Footer.astro` сейчас плейсхолдеры VK и Telegram:
```
vk.com/vsforni, t.me/vsforni
```
Уточни реальные URL аккаунтов в соцсетях.

### Q7 — Текст страницы «История компании» — placeholder

В БД content для id=236 (istoriya-kompanii) пустой: контент был захардкожен
в MODX-шаблоне. На новом сайте стоит placeholder-текст.
Нужно: передать актуальный текст истории компании для замены.

### Q5 — 4 продукта в «Автоматических линиях» используют фото группы

id=262, 270, 272, 274 — у них нет собственного product-image TV,
поэтому используется фото родительской группы. Это принято?

---

## Фаза 2

*(пока пусто)*
